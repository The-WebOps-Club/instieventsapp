<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/events_accents</item>
        <item name="android:windowBackground">@color/white_color_primary</item>
        <item name="android:textColorHint">@color/events_secondary_text</item>
       <!-- <item name="android:textColor">@color/events_text</item>-->
    </style>

    <style name="ToolBarStyle" parent="Theme.AppCompat.Light.NoActionBar">
       <!-- <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>-->
        <item name="android:actionMenuTextColor">@android:color/white</item>

    </style>

</resources>
